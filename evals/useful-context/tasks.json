{
  "version": 1,
  "name": "Satori useful-context repeatable corpus",
  "tasks": [
    {
      "id": "find-search-handler",
      "queryClass": "owner_discovery",
      "language": "typescript",
      "expected": {
        "ownerFile": "packages/mcp/src/core/handlers.ts",
        "ownerSymbol": "handleSearchCode"
      },
      "workload": {
        "setup": [
          {
            "tool": "manage_index",
            "args": { "action": "status", "path": "$REPO_ROOT" }
          }
        ],
        "invocations": [
          {
            "tool": "search_codebase",
            "args": {
              "path": "$REPO_ROOT",
              "query": "where is search code behavior handled",
              "scope": "runtime",
              "resultMode": "grouped",
              "groupBy": "symbol",
              "rankingMode": "auto_changed_first",
              "debugMode": "freshness"
            }
          }
        ],
        "phaseProtocol": {
          "cold": "start a fresh MCP runtime, complete an unmeasured sync with durable status proof, then run the workload once",
          "warm": "repeat the identical workload once while the proven index operation remains unchanged"
        }
      }
    },
    {
      "id": "find-exact-fast-path",
      "queryClass": "exact_identifier",
      "language": "typescript",
      "expected": {
        "ownerFile": "packages/mcp/src/core/search-exact-fast-path.ts",
        "ownerSymbol": "runExactRegistryFastPath"
      },
      "workload": {
        "setup": [
          {
            "tool": "manage_index",
            "args": { "action": "status", "path": "$REPO_ROOT" }
          }
        ],
        "invocations": [
          {
            "tool": "search_codebase",
            "args": {
              "path": "$REPO_ROOT",
              "query": "runExactRegistryFastPath",
              "scope": "runtime",
              "resultMode": "grouped",
              "groupBy": "symbol",
              "rankingMode": "auto_changed_first",
              "debugMode": "freshness"
            }
          }
        ],
        "phaseProtocol": {
          "cold": "start a fresh MCP runtime, complete an unmeasured sync with durable status proof, then run the workload once",
          "warm": "repeat the identical workload once while the proven index operation remains unchanged"
        }
      }
    },
    {
      "id": "open-prepared-cache-owner",
      "queryClass": "exact_open",
      "language": "typescript",
      "expected": {
        "ownerFile": "packages/mcp/src/core/prepared-read-cache.ts",
        "ownerSymbol": "lookupCandidate",
        "span": { "startLine": 68, "endLine": 110 }
      },
      "workload": {
        "setup": [
          {
            "tool": "manage_index",
            "args": { "action": "status", "path": "$REPO_ROOT" }
          }
        ],
        "invocations": [
          {
            "tool": "read_file",
            "args": {
              "path": "$REPO_ROOT/packages/mcp/src/core/prepared-read-cache.ts",
              "mode": "annotated",
              "open_symbol": { "symbolLabel": "lookupCandidate" }
            }
          }
        ],
        "phaseProtocol": {
          "cold": "start a fresh MCP runtime, complete an unmeasured sync with durable status proof, then run the workload once",
          "warm": "repeat the identical workload once while the proven index operation remains unchanged"
        }
      }
    }
  ]
}
